---
title: Webhook
description: 注册 webhook 端点、校验 HMAC-SHA256 签名，并处理 Verify Group 针对实时 API 事件的指数退避重试机制。
---

## 注册 webhook

```bash
POST /api/v1/webhooks
```
```json
{
  "url": "https://your-system.io/webhooks/verify-group",
  "events": ["kyc.verified", "claim.status_changed", "sanctions.match.detected"],
  "secret": "whsec_your_signing_secret"
}
```

## 校验签名

每次投递都会附带 `X-VG-Signature-256`。请务必在处理前先校验：

```typescript
import crypto from "crypto";

function verify(rawBody: string, signature: string, secret: string): boolean {
  const expected = `sha256=${crypto.createHmac("sha256", secret).update(rawBody).digest("hex")}`;
  return crypto.timingSafeEqual(Buffer.from(expected), Buffer.from(signature));
}
```

<Warning>校验前请以 **原始字节** 形式读取 body —— 先解析 JSON 会改变字节表示，从而导致签名校验失败。</Warning>

## 重试策略

投递失败时，我们会按指数退避重试：5s → 30s → 5m → 30m → 2h。连续失败 5 次后，我们会将该 webhook 标记为 `failed`，并向你发送邮件告警。

<Warning>你的端点必须在 **30 秒** 内响应，否则我们会将本次投递判定为失败。</Warning>
