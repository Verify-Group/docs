---
title: إدارة الأدلة
description: ارفع أدلة المطالبات عبر نقطة نهاية الوسيط، ووسم الملفات، وشغّل موافقات المراجعين، وأدر انتقالات حالة الأدلة على Verify Group API.
---

## الرفع (موصى به لعملاء المتصفح)

يوجّه الرفع عبر الخلفية لتجنب مشكلات CORS:

```bash
POST /api/v1/evidence/public-upload/proxy-upload
Content-Type: multipart/form-data
```

يُعيد `evidenceId` و`signedUrl` للمعاينة الفورية.

## سرد الأدلة لمطالبة

```bash
GET /api/v1/evidence?claimId={claimId}
```

## وسم الأدلة

```bash
PATCH /api/v1/evidence/{id}/tags
```
```json
{"tags": ["police_report", "verified", "high_priority"]}
```

## الموافقة على الأدلة أو رفضها

```bash
POST /api/v1/evidence/{id}/review
```
```json
{"status": "APPROVED", "notes": "Document authenticated against police database."}
```

| الحالة | المعنى |
|---|---|
| `PENDING` | بانتظار المراجعة |
| `APPROVED` | تم التحقق منه وقبوله |
| `REJECTED` | مرفوض |
| `UNDER_REVIEW` | قيد التحليل الجنائي |
