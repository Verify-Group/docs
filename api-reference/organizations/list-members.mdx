---
title: "Get organization members"
api: "GET /api/v1/organizations/{id}/members"
description: "Return a paginated list of every user who belongs to an organization, along with their assigned role and current membership status."
---

<ParamField path="id" type="string" required>
  Organization UUID
</ParamField>

<ParamField query="page" type="number">
  Page number (default: 1)
</ParamField>

<ParamField query="limit" type="number">
  Items per page (default: 20)
</ParamField>
